<h1>Edit Your Store</h1>
<%= form_for @store do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, value: @store.name %>
  <%= f.hidden_field :user_id, :value => current_user.id %>

  <%= f.submit %>
<% end %>
