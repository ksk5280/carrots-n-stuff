<table class="table">
  <tr>
    <th>Name</th>
    <th>Price</th>
  </tr>
  <% items.each do |item| %>
    <tr>
      <td><%= link_to item.title, store_item_path(item.store.slug, item) %></td>
      <td><%= format_price(item.price) %></td>
    </tr>
  <% end %>
</table>
