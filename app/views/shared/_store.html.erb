<div class="col-md-4">
  <div class="store-display padded-top">
    <%= link_to image_tag(store.image_url), store_root_path(store.slug)%>
    <div class="item-description text-center">
      <h4><%= link_to "#{store.name}", store_root_path(store.slug) %></h4>
    </div>
  </div>
</div>
